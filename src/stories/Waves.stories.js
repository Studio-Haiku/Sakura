import React from 'react';
import { Waves } from '../Sakura';

export default {
    title: 'SAKURA/WAVES',
    Component: Waves
}

const Template = (args) => <Waves />;

export const Default = Template.bind({});
Default.args = {};
